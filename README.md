## Project

This is a Vite + React + TypeScript + shadcn-ui + Tailwind CSS application.

You can customize this README with details about your own project, domain, deployment setup, and contributor guidelines.

## Getting started

Prerequisites:

- Node.js and npm installed

Install dependencies:

```sh
npm install
```

Start the development server:

```sh
npm run dev
```

Run tests:

```sh
npm test
```

Build for production:

```sh
npm run build
```
